import { Request, Response } from "express";
import { AppDataSource } from "../../data-source";

const RealEstateControllers = async (
  req: Request,
  res: Response
): Promise<Response> => {
  return res.json("nbbb");
};
